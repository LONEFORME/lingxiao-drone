# 凌霄飞控 · AI 速查卡

> 一句话：ANO_LX 凌霄飞控全栈库 —— 官方基线 + 自研空地协同大闭环 + 历年电赛参考工程。
> **详细说明看 `README.md`**。本卡只放"不看就会犯错"的东西。

---

## 铁律

**1. 控制律不在这个仓库里 —— 别找了。**

`FcSrc` 是"输入、输出主程序"。它只做三件事：
①采遥控 + 传感器　②把速度目标打成 **`0x41` 实时控制帧**发给**匿名 IMU 核**　③收 `pwm_to_esc` 输出 PWM。

真正的 PID 姿态 / 位置闭环在 **IMU 核**（`official-guide/docs/04-IMU固件/*.ano`），**固件不公开**。
官方基线 `ANO_LX.c:147` 自带注释：`//############(实时控制帧，自主开发闭环控制，在这里赋值即可)##############`
—— 官方基线本身就是**程控模板**，五方都保留了这段。

**2. IMU 固件 119 版本以上使用新版控制帧**，上位机与飞控固件版本必须协同。

**3. 无人机串口统一 460800**（镭神雷达 / 飞控链路）。锡月方案是 230400，别混。

**4. 🚫 不要重做代码溯源 —— 结论已经有了。**

完整实证结论在 **`../.workbuddy/memory/lingxiao-lineage.md`**（八轮追问的源码级比对结果）。
一句话版：

> 自研 FC = **官方基线骨架** + **设计层继承自 2024-NUEDC-D**（T265 速度替换光流的同槽位开关、ch5 改定点模式）；
> 控制方式与 **UAV-2023 同源**（都从官方模板派生）；与 HUST22 / NUEDC24 只"接口相同、架构不同"。
> 字面层：620 个同名文件中 605 个字节级一致。

⚠️ 字面比对**不足以判定借鉴关系** —— 对"注释重写 + 变量改名"的借鉴会完全失效。
**要细节读那个文件，不要重新跑比对。**

**5. 编译产物**（`.o` / `.axf` / `.d` / `__pycache__`）已在 `.gitignore` 过滤，保持轻量。

---

## 三大支柱

```
official-guide/          官方原本资料
├── docs/                官方手册、通信协议 V7(权威版)、原理图 PCB、IMU 固件
├── firmware-baseline/   凌霄官方纯净源码基线（STM32F407 / MSP432 / TM4C）
├── examples/            官方入门例程（起飞降落、一键航线任务）
└── tools-env/           Keil MDK 安装包、J-Link 驱动、匿名上位机

drone-system/            自研空地协同大闭环
├── companion-computer/  RDK X5 + 树莓派 机载状态机
├── flight-controller/   STM32F407（硬件级倾角防侧翻）
├── edge-vision/         01Studio K230 CyberCAM
├── ground-vehicle/      树莓派网关 + STM32 8 路灰度循线
├── ground-station/      PyQt5 只读遥测 + 中文语音播报
├── specs/               DCP v1 协议
└── demos/               4 段实机飞行视频

reference-projects/      电赛参考工程（**只作参考**）
└── 2024-NUEDC-D / 2022-HUST / UAV-2023
```

**硬件链路**：STM32F407 `/dev/ttyS1` ＋ K230 `/dev/ttyS7` ＋ T265 ＋ 蓝牙 `/dev/bt_serial`

---

## 易混淆

| 不要搞混 | 说明 |
| --- | --- |
| `drone-system/docs/EDC-2026-D-Report.md` | **自研**的 2026 省赛 D 题报告 |
| `reference-projects/2024-NUEDC-D` | **往届参考**工程，不是自研成果 |
| 两者写报告/答辩时**必须分开** | 前者自研，后者参考 |

---

## 关键协议要点（改上位机/飞控通信时看）

- **控制接口** = `rt_tar.st_data.vel_x/vel_y/vel_z`(cm/s) + `yaw_dps`(°/s) → 走 `0x41` 帧
- **速度是机头坐标系**：`vel_x` 永远等于"沿机头方向的速度"，核的 yaw 漂移**不会**让速度指令指错方向
- **`0x41` 速度字段只在"定点模式 + 摇杆中位"时有效**，程控模式下全无效
- **官方帧无校验、无帧类型**；自研补了 XOR 校验 + 三帧类型复用同一串口（`AA 01/02/03`）
- 协议权威文档：`official-guide/docs/01-用户手册与通信协议/匿名通信协议V7.pdf`

细节与真值表 → `../.workbuddy/memory/lingxiao-lineage.md`
